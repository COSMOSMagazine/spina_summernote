console.log('insert image start object_id:<%= params[:page_part_id] %>');

var img = document.createElement('img');
img.src = '<%=  @photo.file.url %>';

var editor = $('#<%= params[:page_part_id] %>');
editor.summernote('restoreRange');
editor.summernote('beforeCommand');
editor.summernote('insertNode', img);
editor.summernote('afterCommand');

$.hideModal();
